name: GridFeatureExtractor2D
backbone_name: vit
backbone_pretrain_dataset: b_32_openai   # 2d feature extractor: clip-vit-large-patch14-336
use_pretrain: True
pooling: avg # null, avg, conv, attn
freeze: True
